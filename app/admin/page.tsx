'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {createClient} from '@/lib/supabase/client';
type O={authorized:boolean;users?:number;picks?:number;public_picks?:number;featured_picks?:number;votes?:number};type P={id:string;slug:string;question:string;status:string;is_public_browse:boolean;is_featured:boolean};
export default function Admin(){const s=createClient(),r=useRouter();const [o,setO]=useState<O|null>(null),[picks,setPicks]=useState<P[]>([]),[role,setRole]=useState(''),[busy,setBusy]=useState('');
async function load(){const {data:{user}}=await s.auth.getUser();if(!user){r.replace('/auth?mode=signin');return}const {data:m}=await s.from('admin_members').select('role').eq('user_id',user.id).maybeSingle();if(!m){setO({authorized:false});return}setRole(m.role);const [{data:a},{data:p}]=await Promise.all([s.rpc('get_admin_overview'),s.rpc('get_admin_picks',{p_limit:100})]);setO(a as O);setPicks((p||[]) as P[])}useEffect(()=>{load()},[]);
async function feed(p:P,pub:boolean,feat:boolean){setBusy(p.id);const {error}=await s.rpc('admin_set_pick_feed',{p_pick_id:p.id,p_public:pub,p_featured:feat});if(error)alert(error.message);await load();setBusy('')}
if(!o)return <main className="shell"><div className="card">Loading Admin Control…</div></main>;if(!o.authorized)return <main className="shell"><div className="card"><h1>Admin Control</h1><p className="error">This account does not have admin access.</p></div></main>;
return <main className="adminShell"><div className="adminTitle"><div><p className="feedEyebrow">PICK CONTROL</p><h1>Admin</h1><p className="muted">{role.toUpperCase()} access</p></div><div className="row"><Link className="primary" href="/admin/machine">PICK Machine</Link><Link className="secondary" href="/create">+ Create PICK</Link></div></div><section className="statGrid"><div className="statCard"><b>{o.users??0}</b><span>Users</span></div><div className="statCard"><b>{o.picks??0}</b><span>PICKs</span></div><div className="statCard"><b>{o.votes??0}</b><span>Votes</span></div><div className="statCard"><b>{o.public_picks??0}</b><span>Public</span></div><div className="statCard"><b>{o.featured_picks??0}</b><span>Featured</span></div></section><section className="adminPanel"><div className="feedHeader"><div><p className="feedEyebrow">FEED CONTROL</p><h2>Recent PICKs</h2></div><button className="secondary" onClick={load}>Refresh</button></div>{picks.map(p=><article className="adminPick" key={p.id}><div className="adminPickBody"><div className="row"><span className={'statusPill '+(p.is_public_browse?'live':'')}>{p.is_public_browse?'PUBLIC':'LINK ONLY'}</span>{p.is_featured&&<span className="statusPill featured">FEATURED</span>}<span className="muted">{p.status}</span></div><h3>{p.question}</h3><Link className="feedLink" href={'/p/'+p.slug}>Open PICK →</Link></div><div className="adminActions">{p.is_public_browse?<button disabled={busy===p.id} className="secondary" onClick={()=>feed(p,false,false)}>Remove from feed</button>:<button disabled={busy===p.id} className="secondary" onClick={()=>feed(p,true,false)}>Make public</button>}{p.is_public_browse&&(p.is_featured?<button className="secondary" onClick={()=>feed(p,true,false)}>Unfeature</button>:<button className="primary" onClick={()=>feed(p,true,true)}>Feature</button>)}</div></article>)}</section></main>}

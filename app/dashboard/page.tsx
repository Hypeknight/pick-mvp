import {createClient} from '@/lib/supabase/server'; import Link from 'next/link'; import {redirect} from 'next/navigation';
export default async function Dashboard(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/auth');const {data}=await s.from('picks').select('id,slug,question,status,created_at,expires_at').eq('creator_id',user.id).order('created_at',{ascending:false});return <main className="shell"><div className="row between"><h1>My PICKs</h1><Link className="primary" href="/create">New PICK</Link></div>{!data?.length?<div className="card"><p className="muted">No PICKs yet. Ask your first question.</p></div>:data.map(p=><Link key={p.id} href={'/p/'+p.slug}><div className="card"><b>{p.question}</b><p className="muted">{p.status} · {new Date(p.created_at).toLocaleString()}</p></div></Link>)}</main>}

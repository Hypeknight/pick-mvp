import {ImageResponse} from 'next/og';import {createClient} from '@/lib/supabase/server';export const size={width:1200,height:630};export const contentType='image/png';
export default async function Image({params}:{params:Promise<{slug:string}>}){const {slug}=await params,s=await createClient();const {data:p}=await s.from('picks').select('question').eq('slug',slug).single(),q=p?.question||'Make the PICK.';return new ImageResponse(<div style={{width:'100%',height:'100%',display:'flex',flexDirection:'column',justifyContent:'space-between',background:'#08080b',color:'white',padding:'64px 72px',fontFamily:'sans-serif'}}><div style={{display:'flex',fontSize:44,fontWeight:800}}>P<span style={{color:'#8b5cf6'}}>I</span>CK</div><div style={{display:'flex',flexDirection:'column',maxWidth:1000}}><div style={{color:'#a78bfa',fontSize:27,fontWeight:700,marginBottom:20}}>HELP ME PICK</div><div style={{fontSize:62,lineHeight:1.05,fontWeight:800}}>{q}</div></div><div style={{display:'flex',justifyContent:'space-between',fontSize:25,color:'#c7c7d1'}}><span>Pick one before you see the crowd.</span><span style={{color:'#a78bfa',fontWeight:700}}>ASK • PICK • REVEAL</span></div></div>,size)}

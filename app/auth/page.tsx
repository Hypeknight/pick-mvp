'use client'; import {FormEvent,useState} from 'react'; import {createClient} from '@/lib/supabase/client';
export default function Auth(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const supabase=createClient();async function submit(e:FormEvent,signup=false){e.preventDefault();setMsg('Working…');const r=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});setMsg(r.error?r.error.message:signup?'Check your email to confirm your account.':'Signed in. You can create a PICK now.');}return <main className="shell"><div className="card"><h1>Sign in to PICK</h1><p className="muted">Anyone can vote. You only need an account to create and manage PICKs.</p><form onSubmit={e=>submit(e)}><input className="field" type="email" placeholder="Email" required value={email} onChange={e=>setEmail(e.target.value)}/><input className="field" type="password" placeholder="Password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/><div className="row"><button className="primary">Sign in</button><button className="secondary" type="button" onClick={e=>submit(e as any,true)}>Create account</button></div></form><p className="muted">{msg}</p></div></main>}
